class SORTTrackers:

    def __init__(self):
        # The core class that runs the SORT algorithm.
        # Manages all tracks: updates them with matches,
        # creates new ones, and removes stale tracks.